 const nome = 'luiz';
 const sobrenome = 'miranda';
 const idade = 30;
//const cpf = 'fdsfgg'

function soma (x, y) {
    return x + y;
};

export default (x, y) => x * y;


export { nome, sobrenome, idade, soma}
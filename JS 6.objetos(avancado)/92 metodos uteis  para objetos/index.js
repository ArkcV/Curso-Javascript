
/*const produto =  {nome : 'produto', preco : 1.80};
const caneca = Object.assign({}, produto, {material: 'porcelana'})

caneca.nome = 'outro nome';
caneca.preco = 22.5;
console.log(produto);
console.log(caneca);*/

/*anotacoes

Object.getQwnPropwetyDescriptor(produto,"nome"); vai mostar os atributos v,w,e,c
Object.defineProperty(produto, 'nome',{ ele retorna essa propriedades abaixo
    writable: false ou true
    configurable : false o true 
    value: 'Qualquer outra coisa'
}


Object.values (retorna os valores)
object.entries (retorna os 2)
Object.keys (retorna as chaves)
Object.freeze (congela o objeto)
Object.defineProperty (define uma propriedade)
Object.defineProperties (define varias propriedades)
Objeto.assign(des, any) (copiar)
...(spread) (copiar)
*/

